# Install required dependencies for the resume thumbnail generator

Write-Host "Installing html2canvas for thumbnail generation..." -ForegroundColor Green
npm install html2canvas

Write-Host "Dependencies installed successfully!" -ForegroundColor Green
Write-Host "You can now use the resume thumbnail generator at /admin/resume-thumbnails" -ForegroundColor Cyan
Write-Host "And the resume template demo with color customization at /resume-templates" -ForegroundColor Cyan